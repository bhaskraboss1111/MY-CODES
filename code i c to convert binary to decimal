#include<stdio.h>
#include<math.h>
int main()
{
    int n;
    scanf("%d",&n);
    int i=0;
    int r=0;
    int rn;
    while(n>0)
    {   rn=n%10;
        r=r+rn*pow(2,i);
        n=n/10;
        i=i+1;
    }
    printf("%d",r);
    return 0;
}
